# Scratch proof file for issue #1245

This file exists only to prove, via a real pull_request event, that a
genuinely doc-only diff (this file matches `docs/**`) now correctly posts
real conclusions for every required status check instead of leaving them
stuck "Expected -- Waiting for status to be reported" forever.

This file and the branches/PR that carry it are disposable and will be
deleted once the proof is recorded.
