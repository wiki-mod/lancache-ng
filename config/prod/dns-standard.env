# lancache-ng (https://github.com/wiki-mod/lancache-ng)
# PowerDNS configuration for standard-mode proxy service (prod deployment).
# IP of the standard-mode proxy (no CA cert required).
# Set to the server's first LAN IP: ip -4 addr show | grep inet
PROXY_IP=192.168.234.10
# PROXY_IPV6=
LOG_QUERIES=0
PDNS_API_KEY=CHANGE_ME_PDNS_API_KEY
DDNS_ALLOW_FROM=
ROOT_ZONE_MIRROR=1
NATS_CONSUMER=dns-standard
NATS_RECONCILER=1

# Known-good configuration snapshot retention (#415, #615). How many
# validated pdns.conf/recursor.conf configs to keep for automatic rollback
# if a newly generated config fails validation at startup. See
# docs/known-good-config-snapshots.md.
KEEP_KNOWN_GOOD_CONFIGS=3
