name: "LANCache CodeQL configuration"

queries:
  - uses: security-extended
  - uses: security-and-quality

paths:
  - services/dns/nats-subscriber/src
  - services/ui/src

paths-ignore:
  - "**/target/**"
