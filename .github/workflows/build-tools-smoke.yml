
# LanCache-NG (https://github.com/wiki-mod/lancache-ng)
# SPDX-License-Identifier: AGPL-3.0-or-later
# What: runs bats/shellspec against published build-tools
# Why: split from build-tools.yml to avoid full rebuilds
# From: Issue #1253 | PR #1501
name: Build Tools Smoke Tests

on:
  push:
    # What: mirrors build-tools.yml's own branches list
    # Why: current_dev is active; v0.2.0 stays frozen
    # From: PR #1501
    branches: [master, current_dev, v0.2.0]
    # What: mirrors check-bats-path-filter-coverage dependencies
    # Why: script is authoritative source for test file selection
    # From: Issue #1095 | PR #1501
    paths:
      - ".github/workflows/build-tools-smoke.yml"
      - "certs/generate-ca.sh"
      - "tests/bats/**"
      - "tests/shellspec/**"
      - "tests/fixtures/**"
      - "setup.sh"
      - "scripts/tracked/check-idempotence-test-coverage.sh"
      - "scripts/tracked/check-validation-subnet-wrapper-coverage.sh"
      - "scripts/tracked/check-bats-path-filter-coverage.sh"
      - "scripts/tracked/check-build-tools-smoke-coverage.sh"
      - "scripts/untracked/check-pipefail-early-exit-grep.sh"
      - "scripts/tracked/check-file-headers.sh"
      - "scripts/tracked/check-pipefail-scope-coverage.sh"
      - "scripts/tracked/check-pr-diff-file-headers.sh"
      - "services/dhcp/**"
      - "scripts/untracked/select-build-tools-image.sh"
      - "scripts/untracked/require-image-platforms.sh"
      - "scripts/tracked/resolve-remote-ref-with-retry.sh"
      - "scripts/lib/**"
      - "scripts/untracked/classify-image-impact.sh"
      - "scripts/untracked/compute-next-release-tag.sh"
      - "scripts/untracked/detect-full-setup-changes.sh"
      - "scripts/untracked/ensure-pr-staging-images.sh"
      - "scripts/untracked/plan-deep-validation.sh"
      - "scripts/untracked/docker-socket-proxy.sh"
      - "scripts/tracked/check-action-node-versions.sh"
      - "scripts/tracked/check-executable-bits.sh"
      - "scripts/tracked/check-workflow-service-lists.sh"
      - "scripts/tracked/check-orphaned-branches.sh"
      - "scripts/tracked/check-changelog-direct-edit.sh"
      - "scripts/tracked/check-pr-title-convention.sh"
      - "scripts/tracked/check-pr-tracking-metadata.sh"
      - "scripts/tracked/check-setup-prompt-drift.sh"
      - "scripts/tracked/check-compose-healthchecks.sh"
      - "scripts/untracked/check-netdata-curl-pin.sh"
      - "scripts/untracked/check-workflow-line-limit.sh"
      - "scripts/untracked/check-review-chronology-comments.sh"
      - "scripts/tracked/check-dependabot-docker-base-consistency.sh"
      - "scripts/untracked/check-trivy-action-direct-usage.sh"
      - "scripts/untracked/check-deny-short-sha.sh"
      - "scripts/untracked/generate-vex.sh"
      - "scripts/untracked/simulations/setup-cli-simulation.sh"
      - "scripts/untracked/simulations/syslog-forwarding-simulation.sh"
      - "scripts/untracked/gc-pr-staging-images.sh"
      - "scripts/untracked/gc-sha-retention-audit.sh"
      - ".github/workflows/gc-pr-staging-images.yml"
      - ".github/workflows/gc-sha-retention-audit.yml"
      - ".github/workflows/backfill-stack-latest.yml"
      - ".github/workflows/build-push.yml"
      - "release/stack-images.yml"
      - ".github/workflows/full-setup-validate.yml"
      - ".github/workflows/full-setup-deep-validate.yml"
      - ".github/workflows/full-setup-sims.yml"
      - ".github/actions/derive-validation-network/action.yml"
      - ".github/actions/wait-validation-stack-health/action.yml"
      - ".github/actions/rust-acceleration-preflight/action.yml"
      - "tools/runner-host/lancache-ci-docker-daemon-config.sh"
      - "services/dns/cdn-domains.txt"
      - "services/dns/entrypoint.sh"
      - "services/dhcp/entrypoint.sh"
      - "services/dhcp/kea-ctrl-agent.conf"
      - "services/dhcp/kea-dhcp4.conf"
      - "services/dhcp/kea-dhcp-ddns.conf"
      - "services/dhcp-proxy/dnsmasq.conf.template"
      - "services/dhcp-proxy/dnsmasq-relay.conf.template"
      - "services/dhcp-proxy/entrypoint.sh"
      - "services/ntp/entrypoint.sh"
      - "services/ntp/chrony.conf"
      - "services/proxy/entrypoint.sh"
      - "services/proxy/conf.d/http.conf"
      - "services/proxy/conf.d/https.conf"
      - "services/proxy/proxy-params.conf"
      - "services/watchdog/watchdog.sh"
      - "services/watchdog/healthcheck.sh"
      - "services/watchdog/retention.sh"
      - "services/proxy/public_suffix_list.dat"
      - "services/proxy/Dockerfile"
      - "services/ui/docker-entrypoint.sh"
      - "services/ui/Dockerfile"
      - "services/netdata/Dockerfile"
      - "services/syslog/netdata-web_log.conf"
      - "services/utilities/Dockerfile"
      - "services/utilities/verify-version-banner.sh"
      - "scripts/untracked/merge-utilities-sbom-components.sh"
      - "deploy/prod/docker-compose.yml"
      - "deploy/quickstart/docker-compose.yml"
      - "deploy/secondary/docker-compose.yml"
      - "deploy/full-setup/docker-compose.yml"
  pull_request:
    # What: mirrors build-tools.yml's own branches list
    # Why: current_dev is active; v0.2.0 stays frozen
    # From: PR #1501
    branches: [master, current_dev, v0.2.0]
    # What: identical to push.paths list above
    # Why: pull_request and push must share file set
    # From: Issue #1095 | PR #1501
    paths:
      - ".github/workflows/build-tools-smoke.yml"
      - "certs/generate-ca.sh"
      - "tests/bats/**"
      - "tests/shellspec/**"
      - "tests/fixtures/**"
      - "setup.sh"
      - "scripts/tracked/check-idempotence-test-coverage.sh"
      - "scripts/tracked/check-validation-subnet-wrapper-coverage.sh"
      - "scripts/tracked/check-bats-path-filter-coverage.sh"
      - "scripts/tracked/check-build-tools-smoke-coverage.sh"
      - "scripts/untracked/check-pipefail-early-exit-grep.sh"
      - "scripts/tracked/check-file-headers.sh"
      - "scripts/tracked/check-pipefail-scope-coverage.sh"
      - "scripts/tracked/check-pr-diff-file-headers.sh"
      - "services/dhcp/**"
      - "scripts/untracked/select-build-tools-image.sh"
      - "scripts/untracked/require-image-platforms.sh"
      - "scripts/tracked/resolve-remote-ref-with-retry.sh"
      - "scripts/lib/**"
      - "scripts/untracked/classify-image-impact.sh"
      - "scripts/untracked/compute-next-release-tag.sh"
      - "scripts/untracked/detect-full-setup-changes.sh"
      - "scripts/untracked/ensure-pr-staging-images.sh"
      - "scripts/untracked/plan-deep-validation.sh"
      - "scripts/untracked/docker-socket-proxy.sh"
      - "scripts/tracked/check-action-node-versions.sh"
      - "scripts/tracked/check-executable-bits.sh"
      - "scripts/tracked/check-workflow-service-lists.sh"
      - "scripts/tracked/check-orphaned-branches.sh"
      - "scripts/tracked/check-changelog-direct-edit.sh"
      - "scripts/tracked/check-pr-title-convention.sh"
      - "scripts/tracked/check-pr-tracking-metadata.sh"
      - "scripts/tracked/check-setup-prompt-drift.sh"
      - "scripts/tracked/check-compose-healthchecks.sh"
      - "scripts/untracked/check-netdata-curl-pin.sh"
      - "scripts/untracked/check-workflow-line-limit.sh"
      - "scripts/untracked/check-review-chronology-comments.sh"
      - "scripts/tracked/check-dependabot-docker-base-consistency.sh"
      - "scripts/untracked/check-trivy-action-direct-usage.sh"
      - "scripts/untracked/check-deny-short-sha.sh"
      - "scripts/untracked/generate-vex.sh"
      - "scripts/untracked/simulations/setup-cli-simulation.sh"
      - "scripts/untracked/simulations/syslog-forwarding-simulation.sh"
      - "scripts/untracked/gc-pr-staging-images.sh"
      - "scripts/untracked/gc-sha-retention-audit.sh"
      - ".github/workflows/gc-pr-staging-images.yml"
      - ".github/workflows/gc-sha-retention-audit.yml"
      - ".github/workflows/backfill-stack-latest.yml"
      - ".github/workflows/build-push.yml"
      - "release/stack-images.yml"
      - ".github/workflows/full-setup-validate.yml"
      - ".github/workflows/full-setup-deep-validate.yml"
      - ".github/workflows/full-setup-sims.yml"
      - ".github/actions/derive-validation-network/action.yml"
      - ".github/actions/wait-validation-stack-health/action.yml"
      - ".github/actions/rust-acceleration-preflight/action.yml"
      - "tools/runner-host/lancache-ci-docker-daemon-config.sh"
      - "services/dns/cdn-domains.txt"
      - "services/dns/entrypoint.sh"
      - "services/dhcp/entrypoint.sh"
      - "services/dhcp/kea-ctrl-agent.conf"
      - "services/dhcp/kea-dhcp4.conf"
      - "services/dhcp/kea-dhcp-ddns.conf"
      - "services/dhcp-proxy/dnsmasq.conf.template"
      - "services/dhcp-proxy/dnsmasq-relay.conf.template"
      - "services/dhcp-proxy/entrypoint.sh"
      - "services/ntp/entrypoint.sh"
      - "services/ntp/chrony.conf"
      - "services/proxy/entrypoint.sh"
      - "services/proxy/conf.d/http.conf"
      - "services/proxy/conf.d/https.conf"
      - "services/proxy/proxy-params.conf"
      - "services/watchdog/watchdog.sh"
      - "services/watchdog/healthcheck.sh"
      - "services/watchdog/retention.sh"
      - "services/proxy/public_suffix_list.dat"
      - "services/proxy/Dockerfile"
      - "services/ui/docker-entrypoint.sh"
      - "services/ui/Dockerfile"
      - "services/netdata/Dockerfile"
      - "services/syslog/netdata-web_log.conf"
      - "services/utilities/Dockerfile"
      - "services/utilities/verify-version-banner.sh"
      - "scripts/untracked/merge-utilities-sbom-components.sh"
      - "deploy/prod/docker-compose.yml"
      - "deploy/quickstart/docker-compose.yml"
      - "deploy/secondary/docker-compose.yml"
      - "deploy/full-setup/docker-compose.yml"

# What: grants only read access to repository contents
# Why: job never writes, so applies least privilege
# From: PR #1501
permissions:
  contents: read

# What: cancels a superseded run for the same workflow+ref
# Why: read-only job hasn't published, so canceling is safe
# From: PR #1501
concurrency:
  group: ${{ format('{0}-{1}', github.workflow, github.ref) }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  bats-shellspec-smoke:
    # What: runs on the light tier, not heavy
    # Why: pulls digest and runs suites per light tier contract
    # From: PR #1501
    name: run bats/shellspec suites against the published build-tools image
    runs-on: [self-hosted, linux, lancache, lancache-light]
    # What: sets a generous timeout well above observed runtime
    # Why: headroom covers image pull and test suites under load
    # From: PR #1501
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # What: sets up Docker Buildx for manifest inspection
      # Why: resolves channel tag to digest via buildx imagetools
      # From: PR #1501
      - name: Set up Docker Buildx for manifest inspection
        id: buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      # What: authenticates published-image pull with credentials
      # Why: avoids GHCR's lower anonymous rate limit under CI load
      # From: PR #1501
      - name: Select published build-tools image
        id: select-image
        env:
          GITHUB_EVENT_PULL_REQUEST_HEAD_REPO_FULL_NAME: ${{ github.event.pull_request.head.repo.full_name }}
          GHCR_RETRY_USERNAME: ${{ github.actor }}
          GHCR_RETRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          # What: sets BUILD_TOOLS_REQUIRE_PUBLISHED=true
          # Why: forces strict pull-only path for this light-tier job
          # From: PR #1501
          image="$(BUILD_TOOLS_REQUIRE_PUBLISHED=true bash scripts/untracked/select-build-tools-image.sh)"
          case "$image" in
            *@sha256:*)
              ;;
            *)
              echo "::error::Expected a digest-qualified build-tools image from the published build-tools selector, got '$image'."
              exit 1
              ;;
          esac
          printf 'BUILD_TOOLS_IMAGE=%s\n' "$image" >> "$GITHUB_ENV"

      # What: mirrors identically-named step against published image
      # Why: light-tier job must stay pull-only, never build locally
      # From: PR #1501
      - name: Run Bats setup fixtures
        timeout-minutes: 20
        run: |
          set -euo pipefail

          # What: runs with init process as container PID 1
          # Why: without PID-1 reaper, zombie processes remain alive
          # From: Issue #1522 | PR #1523
          docker run --rm --init \
            --user "$(id -u):$(id -g)" \
            -v "$PWD:/work:ro" \
            -w /work \
            "${BUILD_TOOLS_IMAGE:?BUILD_TOOLS_IMAGE is required}" \
            bash -lc 'set -euo pipefail; bats tests/bats'

      # What: mirrors identically-named ShellSpec step
      # Why: keeps light-tier job pull-only and consistent
      # From: PR #1501
      - name: Run ShellSpec setup fixtures
        timeout-minutes: 20
        run: |
          set -euo pipefail

          # What: uses init-as-PID-1 container like Bats step
          # Why: keeps fixture runners on same lifecycle contract
          # From: Issue #1522 | PR #1523
          docker run --rm --init \
            --user "$(id -u):$(id -g)" \
            -v "$PWD:/work:ro" \
            -w /work \
            "${BUILD_TOOLS_IMAGE:?BUILD_TOOLS_IMAGE is required}" \
            bash -lc 'set -euo pipefail; shellspec --shell bash tests/shellspec'
